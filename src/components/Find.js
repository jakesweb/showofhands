// find a poll created by a user to vote in
import React from "react";
import styled from "styled-components";

const Find = () => (
  <div>
    <p>Find!</p>
  </div>
);

export default Find;
